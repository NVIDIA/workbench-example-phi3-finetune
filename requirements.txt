# Pinned against the nvcr.io/nvidia/pytorch:25.09-py3 base (torch 2.9 / CUDA 13, Python 3.12).
# bitsandbytes publishes no linux-aarch64 wheel before 0.46.0, and every release that has one
# requires torch>=2.2, so the previous pins cannot be installed on arm64 at all.
# transformers must clear the releases that import `apex.amp`, which the NGC base no longer exports.
# datasets stays below 4.0, which dropped the `trust_remote_code` the notebook passes.
# wandb 0.17 uses np.float_, removed in the NumPy 2 this base ships.
# numpy is left unpinned: the base ships NumPy 2, and forcing 1.26 over it breaks the NGC torch.
# trl is dropped; the notebook never imports it.
jupyterlab>3.0
datasets==3.6.0
bitsandbytes==0.50.2
transformers==4.56.2
peft==0.17.1
accelerate==1.10.1
wandb==0.22.2
einops==0.7.0
scipy==1.14.0
sentencepiece==0.2.0
ipywidgets
